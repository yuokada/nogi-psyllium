---
applyTo: "data/**/*.yaml,public/data/**/*.json"
---

データファイルを編集する場合は、以下を守ってください。

- `data/*.yaml` を正本として扱い、必要がない限り `public/data/*.json` を直接編集しない。
- メンバー ID やアンダーライブ ID は既存データとの整合性を保つ。
- 仕様変更がデータ構造に及ぶ場合は、関連コンポーネントや README も同じ変更で見直す。
